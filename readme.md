# Django AI School Scheduler

Проект Django для управления школьным расписанием с помощью AI (Google Gemini).
Позволяет добавлять, удалять и изменять уроки, кабинеты и учителей через текстовые команды.

---

## 📦 Структура проекта

```
my_project/
├─ manage.py
├─ my_app/
│  ├─ models.py         # Модели: Teacher, Room, Lesson
│  ├─ services.py       # Класс ScheduleAIService
│  ├─ views.py          # API для команд AI
│  └─ ...
├─ templates/
├─ static/
├─ requirements.txt
└─ README.md
```

---

## 🧩 Модели

* **Teacher** – учитель

  * `id` (CharField, PK)
  * `name`, `subject`, `email`

* **Room** – кабинет

  * `id` (CharField, PK)
  * `number`, `capacity`, `room_type`

* **Lesson** – урок

  * `id` (CharField, PK)
  * `teacher` (FK → Teacher)
  * `room` (FK → Room)
  * `subject`, `day`, `slot`, `grade`, `color`

---

## ⚙ Установка

1. Клонируем репозиторий:

```bash
git https://github.com/Fakh004/AI-Timetable-Service.git
cd django-ai-scheduler
```

2. Создаем виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # Linux / Mac
venv\Scripts\activate     # Windows
```

3. Устанавливаем зависимости:

```bash
pip install -r requirements.txt
```

4. Создаем `.env` файл:

```
GEMINI_API_KEY=ваш_ключ_Google_Gemini
DEBUG=True
SECRET_KEY=ваш_секретный_ключ
```

5. Применяем миграции:

```bash
python manage.py migrate
```

6. Создаем суперпользователя:

```bash
python manage.py createsuperuser
```

---

## 🚀 Запуск

```bash
python manage.py runserver
```

API доступно по адресу:

```
POST /api/ai-command/
Content-Type: application/json
Body:
{
  "command": "Добавь урок Математика для 10А на понедельник 2-й урок в кабинет 101"
}
```

---

## 📝 Использование AI команд

Примеры команд:

* Добавление урока:

  ```
  Добавь урок Математика для 10А на понедельник 2-й урок в кабинет 101
  ```

* Удаление урока по предмету:

  ```
  Удали урок Физика
  ```

* Удаление всех уроков класса:

  ```
  Удали уроки 10А
  ```

* Добавление нового учителя:

  ```
  Добавь учителя
  ```

* Добавление нового кабинета:

  ```
  Добавь кабинет 202
  ```

> 💡 Если AI недоступен (лимит free tier), проект автоматически использует встроенный парсер команд.

---

## 🧠 AI

* Используется **Google Gemini API** (`gemini-2.5-flash`) для генерации расписания.
* AI создает учителей, кабинеты и уроки с уникальными строковыми ID (`t1`, `r1`, `l1`).
* При превышении лимита free tier используется локальный парсер (`_parse_command`).

---

## ⚠ Ограничения

* Free tier Google Gemini: **20 запросов в день**.
* Строгие ограничения на уникальность уроков, кабинетов и учителей должны соблюдаться в AI.
* Все данные сохраняются в базе Django, поэтому список уроков всегда актуален.

---

## 🛠 Разработка

* Создание тестовых уроков вручную:

```python
from my_app.models import Teacher, Room, Lesson

Teacher.objects.create(id="t1", name="Иванов И.И.", subject="Математика")
Room.objects.create(id="r1", number="101", capacity=30)
Lesson.objects.create(id="l1", teacher_id="t1", room_id="r1", subject="Математика", day="Monday", slot=2, grade="10А", color="blue")
```

* Проверка текущего состояния:

```python
from my_app.services import ScheduleAIService

service = ScheduleAIService()
state = service.get_current_state()
print(state['lessons'])
```

---

## 📄 Лицензия

MIT License

---

## 🔗 Ссылки

* [Документация Gemini API](https://ai.google.dev/gemini-api/docs)
* [Мониторинг квот](https://ai.dev/usage?tab=rate-limit)
